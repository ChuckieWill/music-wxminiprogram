<!-- 歌单列表-----首页-----音乐 -->
<view class="plitem" bindtap="goTomusiclist">
  <image src="{{playlist.picUrl}}" class="plitemImage"></image>
  <text class="plitemCount">{{_count}}</text>
  <view class="plitemName">{{playlist.name}}</view>
</view>
